spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    open-in-view: false
    hibernate:
      #      ddl-auto: update
      show_sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
        format_sql: true
